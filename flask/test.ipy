import sys
import json

data = {    
            "username":"testuser",
            "title":"test note title",
            "noteList": [
                            {"text":"test text 1"},
                            {"text":"test text 2"}
                        ],
            "x":0,
            "y":0,
            "noteID":0,
            "color": "#ffffff"
       }

data = "'"+str(data)+"'"

# add_note
test = !curl \
	    -X POST \
	    -H "Content-Type: application/json" \
	    -d $data \
	    http://localhost:5000/add_note
print(test)


