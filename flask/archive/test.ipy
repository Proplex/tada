test = !curl \
	    -X POST \
	    -H "Content-Type: application/json" \
	    -d '' \
	    http://localhost:5000/add
print(test)

test = !curl \
	    -X POST \
	    -H "Content-Type: application/json" \
	    -d '' \
	    http://localhost:5000/add
print(test)

test = !curl \
	-X POST \
	-H "Content-Type: application/json" \
	-d '}' \
	http://localhost:5000/add
print(test)

test = !curl \
	-X POST \
	-H "Content-Type: application/json" \
	-d '{"username":"testuser"}' \
	http://localhost:5000/login
print(test)
